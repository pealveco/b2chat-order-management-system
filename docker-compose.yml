services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  app:
    build:
      context: .
      dockerfile: deployment/Dockerfile
    ports:
      - "${SERVER_PORT:-8080}:8080"
    environment:
      SERVER_PORT: ${SERVER_PORT}
      SPRING_R2DBC_URL: ${SPRING_R2DBC_URL}
      SPRING_R2DBC_USERNAME: ${SPRING_R2DBC_USERNAME}
      SPRING_R2DBC_PASSWORD: ${SPRING_R2DBC_PASSWORD}
      SPRING_REDIS_HOST: ${SPRING_REDIS_HOST}
      SPRING_REDIS_PORT: ${SPRING_REDIS_PORT}
      JWT_SECRET: ${JWT_SECRET}
      JWT_ISSUER_URI: ${JWT_ISSUER_URI}
      JWT_CLIENT_ID: ${JWT_CLIENT_ID}
      JWT_JSON_EXP_ROLES: ${JWT_JSON_EXP_ROLES}
      JWT_EXPIRATION_MINUTES: ${JWT_EXPIRATION_MINUTES}
      ADAPTERS_R2DBC_HOST: ${ADAPTERS_R2DBC_HOST}
      ADAPTERS_R2DBC_PORT: ${ADAPTERS_R2DBC_PORT}
      ADAPTERS_R2DBC_DATABASE: ${ADAPTERS_R2DBC_DATABASE}
      ADAPTERS_R2DBC_SCHEMA: ${ADAPTERS_R2DBC_SCHEMA}
      ADAPTERS_R2DBC_USERNAME: ${ADAPTERS_R2DBC_USERNAME}
      ADAPTERS_R2DBC_PASSWORD: ${ADAPTERS_R2DBC_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
